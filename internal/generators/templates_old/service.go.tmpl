package {{.ModuleName}}

import (
    "context"
    "errors"
    "github.com/valdirsb/artemis/pkg/contracts"
)

type {{.ModuleName}}Service struct {
    repository contracts.{{.ModuleName}}Repository
}

func New{{.ModuleName}}Service(repo contracts.{{.ModuleName}}Repository) *{{.ModuleName}}Service {
    return &{{.ModuleName}}Service{
        repository: repo,
    }
}

func (s *{{.ModuleName}}Service) Create(ctx context.Context, req contracts.Create{{.ModuleName}}Request) (*contracts.{{.ModuleName}}, error) {
    // Validate the request
    if req.Name == "" {
        return nil, errors.New("name is required")
    }

    // Create the entity
    entity := &contracts.{{.ModuleName}}{
        Name: req.Name,
        // Set other fields as necessary
    }

    // Persist the entity
    if err := s.repository.Create(ctx, entity); err != nil {
        return nil, err
    }

    return entity, nil
}

// Additional service methods can be added here
